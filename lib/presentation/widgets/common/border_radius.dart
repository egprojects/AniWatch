import '../widgets.dart' show BorderRadius, Radius;

const BorderRadius borderRadius24 = BorderRadius.all(Radius.circular(24.0));

const BorderRadius borderRadiusB24 =
    BorderRadius.vertical(bottom: Radius.circular(24.0));
